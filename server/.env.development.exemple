# Development Environment (MacBook + SQLite)
DATABASE_PROVIDER="sqlite"
DATABASE_URL="file:./dev.db"

# JWT
JWT_SECRET="dev-jwt-secret-change-in-production"

# Server
PORT=3001
NODE_ENV=development

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000