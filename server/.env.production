# Production Environment (LXC + PostgreSQL)
DATABASE_PROVIDER="postgresql"
DATABASE_URL="postgresql://pf360_user:CHANGE_PASSWORD@CHANGE_POSTGRES_IP:5432/pf360?schema=public"

# JWT
JWT_SECRET="CHANGE_THIS_IN_PRODUCTION"

# Server
PORT=3001
NODE_ENV=production

# Client URL (for CORS)
CLIENT_URL=https://CHANGE_DOMAIN.com